class Solution {
public:
    int maxProduct(vector<int>& nums) {
        vector<int> a(INT_MIN, nums.size());
        a[0] = nums[0];
        for(unsigned i = 1; i < nums.size(); i++){
            if()
        }
    }
};
